#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

char* createFile();
void fillFile(char*);
void parseStr(char*, const char*, const char*);
int numStrInFile(const char*);
void fillDictionary(char*);